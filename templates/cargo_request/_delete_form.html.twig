<form method="post" action="{{ path('cargo_request_delete', {'id': cargo_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cargo_request.id) }}">
    <button class="btn">Delete</button>
</form>
